<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingban.futures.dao.PredictAccuracyHistoryDaysMapper">
	
	<select id="getPredictAccuracySummary">
		SELECT SUM(num) num, SUM(correct) correct, SUM(correct)/SUM(num), CODE accuracy 
		FROM futures_predict_accuracy_history_days 
		<where>
			<if test="code!=null">
				AND code = #{code}
			</if>
			<if test="startDate!=null">
				AND createTime &gt;= #{startDate}
			</if>
			<if test="endDate!=null">
				AND createTime &lt;= #{endDate}
			</if>
		</where>
		GROUP BY code
	</select>
	
</mapper>
